# Agent API

LangGraph-based backend responsible for intent understanding, planning, MCP tool selection, result aggregation, ranking, and summarization.

Planned scope:

- Connect to an LLM.
- Call the MCP BoondManager server.
- Return structured results and concise reasoning to the web UI.
