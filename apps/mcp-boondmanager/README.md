# MCP BoondManager

Spring Boot MCP server exposing BoondManager endpoints as deterministic MCP tools.

Planned scope:

- Handle authentication.
- Validate tool inputs.
- Manage pagination and API errors.
- Normalize BoondManager responses.

This service should not perform LLM reasoning, ranking, or summarization.
